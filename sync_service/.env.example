# Convex Configuration
CONVEX_URL=https://your-project.convex.cloud

# MQTT Configuration (defaults are shown, uncomment to override)
# MQTT_BROKER=mqtt://mqtt.meshtastic.org
# MQTT_TOPIC=msh/US/2/#

# Optional: Filter for specific device IDs (comma-separated)
# DEVICE_ID_FILTER=!1234abcd,!5678efgh

# Optional: Deduplication threshold in meters (default: 2)
# DEDUPE_THRESHOLD_METERS=2

# Optional: Enable debug logging
# DEBUG=true
